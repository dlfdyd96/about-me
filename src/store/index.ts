import { createStore } from "vuex";

export interface PortfolioI {
  image: string;
  title: string;
  contents: string;
}

export default createStore({
  state: {},
  mutations: {},
  actions: {},
  modules: {},
});
